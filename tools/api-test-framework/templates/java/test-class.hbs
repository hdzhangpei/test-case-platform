package {{packageName}};

{{#each imports}}
import {{this}};
{{/each}}

import lombok.extern.slf4j.Slf4j;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.junit4.SpringRunner;

/**
 * 自动生成的 API 测试类 — 请勿手动修改
 *
 * Service: {{serviceName}}
 * Generated by: @digital-user/api-test-framework
 */
@Slf4j
@RunWith(SpringRunner.class)
@SpringBootTest(classes = { {{bootstrapSimple}}.class})
@ActiveProfiles("{{activeProfile}}")
public class {{className}} extends ApiTestBase {

    @Autowired
    private {{serviceName}} {{serviceField}};

{{#each methods}}
    /**
     * Case: {{caseId}}
     * Name: {{caseName}}
     * Priority: {{priority}} | Group: {{group}}
     * {{mvnCommand}}
     */
    @Test
    public void {{methodName}}() {
        // setup
        {{setupCode}}

        // input
        {{inputCode}}

        // invoke
        {{invocationCode}}

        // assert
        {{assertionCode}}

        // log
        {{logCode}}
    }

{{/each}}
}
